OK. Here's the full clean restart, step by step. Run each command exactly, and paste me the output if anything fails.

---

**1. Remove any old broken install target**

```bash
rm -rf ~/autopsy
```

**2. Install the Sleuth Kit Java bindings deb (already downloaded)**

```bash
sudo dpkg -i ~/sleuthkit-java_4.14.0-1_amd64.deb
```

If it errors about dependencies:

```bash
sudo apt-get install -f -y
```

**3. Verify the jar is in place**

```bash
ls /usr/share/java/sleuthkit-4.14.0.jar
```

Stop here and confirm you see the file before continuing.

**4. Set Java home and re-run the installer**

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
cd ~/autopsy-4.22.1
bash linux_macos_install_scripts/install_application.sh
```

**5. Verify the install created the launch script**

```bash
ls ~/autopsy/bin/autopsy
```

**6. Launch Autopsy**

```bash
~/autopsy/bin/autopsy
```

**7. Inside Autopsy GUI — add the image at:**

```
/mnt/shared/lubuntu_forensic.dd
```

Start with Step 2 since the deb is already sitting in your home directory. Show me what Step 2 and Step 3 output.
